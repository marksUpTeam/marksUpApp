package ru.bmstu.marksUpTeam.android.marksUpApp.data

/*class LessonRepository(private val lessonApi:LessonApi) {
    suspend fun getStudents():List<Student>{
        return lessonApi.getStudent("jwt").body() ?: emptyList()
    }
    suspend fun getDisciplines():List<Discipline>{
        return lessonApi.getDiscipline("jwt").body() ?: emptyList()
    }
    suspend fun getProfile():Profile{
        return lessonApi.getProfile("jwt").body() ?: baseTeacherProfile
    }
} */